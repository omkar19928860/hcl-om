
trigger:
- main

pool: omkar-hcl

stages:
 - stage: install
   jobs: 
    - job: installterraform
      steps:
      - task: TerraformInstaller@1
        inputs:
          terraformVersion: 'latest'

  
